interface:
  display_name: "Zasi"
  short_description: "Repo-specific patterns and workflows for zasi"
  default_prompt: "Use the zasi repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true